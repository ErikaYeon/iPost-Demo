export default class APIConstants {
  static readonly LIST_LIMIT: number = 10;
  static readonly OLD_DATE: string = "2022-11-10T14:30:00Z"; // ToDo: change for a persisted one
  static readonly TIME_OUT: number = 10; //ToDo: config and use it
  static readonly MAX_RETRIES: number = 3; //ToDo: config and use it
}
